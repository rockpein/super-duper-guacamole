import sqlite3

connectionMovies = sqlite3.connect("main_database.db")
cMovies = connectionMovies.cursor()

cMovies.execute("SELECT title, genres, vote_average, vote_count, adult FROM movie_database_c WHERE release_date = (?) and vote_count > 1000 ORDER BY vote_average DESC LIMIT 100", (int(release_year),))
result = cMovies.fetchall()
print(result)